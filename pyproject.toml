[project]
name = "metadataskritp"
<<<<<<< HEAD
version = "0.2.0"
description = "Skript zum Ergänzen von Metadaten ausgehend von artist & titel"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mutagen>=1.47.0",
    "requests>=2.32.5",
    "watchdog>=6.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
=======
version = "0.3.0"
description = "Tool zum downloaden und taggen eigener Musik"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beets>=2.7.1",
    "pytest>=9.0.2",
    "rapidfuzz>=3.14.3",
    "ruff>=0.15.6",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
>>>>>>> 666dd07 (WIP restructoring and changing API-Endpoints)
